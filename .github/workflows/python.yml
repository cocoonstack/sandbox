name: python

on:
  push:
    paths:
      - "sdk/python/**"
      - "sdk/openai/**"
      - "protocol/**"
      - ".github/workflows/python.yml"
  pull_request:
    paths:
      - "sdk/python/**"
      - "sdk/openai/**"
      - "protocol/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install tooling
        run: pip install ruff pytest && pip install -e sdk/python -e sdk/openai
      - name: Lint
        run: ruff check sdk/python && ruff check sdk/openai
      - name: Test cocoonsandbox
        run: cd sdk/python && pytest -q
      - name: Test openai adapter
        run: cd sdk/openai && pytest -q
