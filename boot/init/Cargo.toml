[package]
name = "sandbox-init"
version = "0.1.0"
edition = "2021"
description = "Initramfs PID 1: assemble the EROFS+overlay rootfs and switch_root"
publish = false

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
