[package]
name = "sandbox-shim"
version = "0.1.0"
edition = "2021"
description = "PID 1 for one blink: fork cocoon-agent, then exec systemd"
publish = false

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
